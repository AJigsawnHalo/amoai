# Discord bot
discord.py==2.7.1

# Async HTTP client (core/llm.py <-> Ollama, core/embeddings.py) + sync HTTP (tools using requests)
aiohttp==3.14.2
requests==2.34.2

# .env config loading (used throughout core/config.py, tools/, email-monitor/, log-monitor/)
python-dotenv==1.2.2

# System stats for tools/server_status.py
psutil==7.2.2

# PDF text extraction for file attachments (core/attachments.py) and tools/rag_knowledge.py
# pypdf is the primary reader; PyPDF2 is a deliberate fallback used only if
# pypdf fails to import (see _extract_pdf_text in core/attachments.py) — keep both.
pypdf==6.14.2
PyPDF2==3.0.1

# Google Calendar integration (tools/calendar_tool.py, setup_google_calendar_auth.py)
google-api-python-client==2.198.0
google-auth==2.56.0
google-auth-oauthlib==1.4.0
